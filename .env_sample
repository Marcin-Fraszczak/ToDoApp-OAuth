# Only needed for Google authentication:
GOOGLE_CLIENT_ID="{generated from your console: https://console.cloud.google.com/}"
GOOGLE_CLIENT_SECRET="{generated from your console: https://console.cloud.google.com/}"

# OAuth flow:
SECRET_KEY="{run the command: openssl rand -hex 32}"
ACCESS_TOKEN_LIFETIME_SECONDS=30
REFRESH_TOKEN_LIFETIME_SECONDS=600

# Connection with mongodb cluster:
MONGODB_CONNECTION_STRING="from your mongodb settings"

# For email sending functionality
MAIL_USERNAME="john.doe@gmail.com"
MAIL_PASSWORD=<most likely need to generate app password for this -> go to your Google account>
MAIL_FROM="showing sender"
MAIL_PORT=587
MAIL_SERVER="smtp.gmail.com"
MAIL_FROM_NAME="like name of your app or whatever"
MAIL_STARTTLS=True
MAIL_SSL_TLS=False
USE_CREDENTIALS=True
VALIDATE_CERTS=True

# If publishing, change it to frontend address:
LOCAL_HOST='http://127.0.0.1:3000'